% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leximax.R
\name{sfa_lexmap}
\alias{sfa_lexmap}
\title{Build the Lexical Map for Leximax Rotation}
\usage{
sfa_lexmap(
  x,
  model = NULL,
  instruction = NULL,
  pool = NULL,
  block_size = 50000L,
  ...
)
}
\arguments{
\item{x}{An \code{\link[=sfa]{sfa()}} fit, or a character vector of item texts.}

\item{model}{Naming model id. Default \code{NULL} follows \code{\link[=sfa_name]{sfa_name()}}:
the fit's embedding model (or the package default) is used.}

\item{instruction}{Naming instruction override (see
\code{\link[=sfa_naming_instruction]{sfa_naming_instruction()}}).}

\item{pool}{An \code{\link[=sfa_pool]{sfa_pool()}} object, or \code{NULL} to fetch it.}

\item{block_size}{Pool rows per block while building S (memory knob;
results are identical for any value).}

\item{...}{Passed to the embedding backend.}
}
\value{
An object of class \code{sfa_lexmap}.
}
\description{
Precomputes everything leximax needs to score orientations of one
instrument: the instruction-conditioned item embeddings, their Gram
matrix, and the pool-by-item cosine matrix S. Building the map costs one
pass over the candidate pool; every subsequent naming of any orientation
of the same items is then nearly instant, which is what makes multi-start
optimization and Monte Carlo calibration practical.
}
